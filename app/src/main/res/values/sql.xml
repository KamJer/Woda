<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="sql_drop_table">DROP TABLE IF EXISTS TempTable;</string>
    <string name="sql_create_temp_table">CREATE TEMP TABLE IF NOT EXISTS TempTable AS
        SELECT id, waterDayId, typeId, SUM(waterDrank) AS waterDrank
        FROM water
        GROUP BY waterDayId, typeId;</string>
    <string name="sql_delete_from_water">DELETE FROM water;</string>
    <string name="sql_insert_into_water_from_temp">INSERT INTO water (id, waterDayId, typeId, waterDrank)
        SELECT id, waterDayId, typeId, waterDrank
        FROM TempTable;</string>
    <string name="sql_delete_type_where_id">DELETE FROM type WHERE id = ?;</string>
    <string name="sql_insert_into_water_values">INSERT OR REPLACE INTO water (id, waterDrank, typeId, waterDayId) VALUES (?)</string>
</resources>