<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="10dp"
    >

    <SeekBar
        android:id="@+id/redSeekBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="255"
        android:padding="10dp"
        android:progressTint="@color/red"
        android:thumbTint="@color/red"
        />

    <SeekBar
        android:id="@+id/greenSeekBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="255"
        android:padding="10dp"
        android:progressTint="@color/green"
        android:thumbTint="@color/green"
        />

    <SeekBar
        android:id="@+id/blueSeekBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="255"
        android:padding="10dp"
        android:progressTint="@color/blue"
        android:thumbTint="@color/blue"
        />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center">

        <ImageButton
            android:id="@+id/colorIndicatorImageView"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_centerInParent="true"
            android:background="@drawable/color_type_button_shape"
            android:contentDescription="button for accepting picked color"
            android:padding="10dp"
            />

        <ImageView
            android:id="@+id/acceptImage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/accept"
            android:layout_centerInParent="true"
            android:scaleType="centerCrop"
            android:contentDescription="image to make button for accepting picked color more readeable"
            />
    </RelativeLayout>


</LinearLayout>